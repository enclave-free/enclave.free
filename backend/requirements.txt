# FastAPI and server
# MAL-2026-4750 was a false positive for FastAPI; keep the upper bound open.
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.27

# Keep the client on the server minor pinned in docker-compose.infra.yml.
qdrant-client>=1.12.2,<1.13.0

# Embeddings (CPU-friendly, multilingual)
sentence-transformers>=5.1.2
torch==2.8.0
transformers>=4.57.6

# Document processing
docling>=2.5.2
PyMuPDF>=1.26.7
reportlab==5.0.0

# Utilities
pydantic>=2.6.0,<3.0.0
python-dotenv>=1.2.2
numpy<2
itsdangerous>=2.0.0
urllib3>=2.7.0
requests>=2.33.0
Pillow>=12.2.0
filelock>=3.20.3

# Nostr signature verification (BIP-340 Schnorr)
coincurve>=20.0.0

# NIP-04 encryption (AES-256-CBC)
pycryptodome>=3.20.0

# NIP-19 bech32 decoding (npub)
bech32>=1.2.0

# LLM provider (OpenAI-compatible SDK)
openai>=1.12.0
httpx>=0.25.0

# Shared rate limiting store (Valkey speaks the Redis protocol)
redis>=5.0.0,<6.0.0
