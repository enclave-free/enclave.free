# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Qdrant client (pinned to match server version 1.7.4)
qdrant-client>=1.7.3,<1.8.0

# Embeddings (CPU-friendly, multilingual)
sentence-transformers>=2.3.1
torch>=2.1.2
transformers>=4.36.0

# Document processing
docling>=2.5.2
PyMuPDF>=1.23.0

# Utilities
pydantic>=2.6.0,<3.0.0
python-dotenv>=1.0.0
numpy<2
itsdangerous>=2.0.0

# Nostr signature verification (BIP-340 Schnorr)
coincurve>=20.0.0

# NIP-04 encryption (AES-256-CBC)
pycryptodome>=3.20.0

# NIP-19 bech32 decoding (npub)
bech32>=1.2.0

# LLM provider (OpenAI-compatible SDK)
openai>=1.12.0
httpx>=0.25.0

# Shared rate limiting store (Valkey speaks the Redis protocol)
redis>=5.0.0,<6.0.0
