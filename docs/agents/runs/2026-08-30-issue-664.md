# Issue 664 session

## Issue

- Issue: #664, Localize magic-link email in the selected advertised locale
- Fixed point before session: `64195eb5ee2b735c8432bc8a42888e6d83795858`
- Worker session: `/root/ticket_664`
- Commit: `520891a587f97e70910278fed0f4ed232a137940`
- Follow-up commit: `27113d417fe7b80d85f73ab812fa759ad5f0b279`
- Status: review follow-up complete; backend-only follow-up commit pending parent handoff

## Inputs

- Spec issue: #659
- Ticket: #664
- Relevant glossary terms: Advertised Locale, User language, Instance
- Relevant ADRs: authentication and User Invitation decisions in `CONTEXT.md`
- Prototype answer and source branch, if any: none

## Implementation

- Public interface used: magic-link request locale and pure localized email renderer
- Behaviors covered: exact 31-locale translation coverage and placeholder parity; explicit locale normalization with English fallback; localized subject, heading, explanation, button, expiry, unsolicited guidance, and copy-link copy; HTML escaping and RTL direction; request propagation; neutral response and token/lifetime regression coverage
- `tdd` used: yes, at request validation/defaulting, pure template rendering, escaping, and endpoint propagation seams (frontend request propagation intentionally deferred to concurrent #661)
- Commands run during implementation:
  - `PYTHONPATH=backend/app python3 -m unittest ...` pure renderer tests and `python3 -m py_compile ...` (passed; host Python 3.9 cannot import the repo's existing `str | None` annotations)
  - Containerized Python 3.11: `python -m unittest discover -s /tests -p 'test_magic_link*.py' -v` (11 passed)
  - Containerized Python 3.11 targeted auth/security set (magic-link localization/enumeration, input validation, rate limiting, deployment config): 58 passed
  - Containerized Python 3.11 backend security regression set including localization: 141 passed
- Full suite command: backend auth/security tests plus relevant frontend request tests (frontend request propagation deferred until #661 handoff)
- Translation provenance: all 30 non-English locale entries were machine-assisted by the Codex worker/model used in this session (GPT-5/Codex runtime; direct `apply_patch` implementation, no Ollama invocation) in one grouped translation-data pass on 2026-08-30. Structural review covered exact `Formatter.parse` placeholder parity, all-locale rendering, and Spanish/Arabic/Czech spot checks. Native-speaker Linguistic Review remains outstanding; this is not a human-quality translation claim.

## Review

- Review fixed point: `520891a587f97e70910278fed0f4ed232a137940`
- Standards findings: follow-up fixes applied; pending final independent review
- Spec findings: follow-up fixes applied; pending final independent review
- Worthy fixes applied: plain sanitized subject identity; exact `Formatter.parse` placeholder validation; bidi isolation for mixed identity and URL copy
- Findings ignored with reasons: host Python 3.9 cannot evaluate the repository's pre-existing Python 3.10 union annotations; containerized Python 3.11 passed the relevant suites

## Risks

- Authentication, rate limiting, neutral enumeration, token contents, and lifetime must remain unchanged.
- The server accepts only an explicit advertised locale; it never infers locale from email headers or browser defaults.
- Automated translation establishes coverage but remains subject to Linguistic Review.

## Frontend follow-up

- Worker session: `/root/ticket_664_frontend`
- Fixed point: `77801fdda92d1e981abd931c46bff93683eb3538` (reviewed parent branch with the backend renderer)
- Scope: propagate the persisted explicit User locale from `UserAuth` to `POST /auth/magic-link` without using browser-detected language state.
- Implementation: `getExplicitLanguageChoice()` derives its allowed values from the typed advertised locale catalog; missing, invalid, or unavailable storage returns `undefined`, leaving the request at the backend's reviewed English fallback. Existing email/name, response, token, and authentication behavior is unchanged.
- TDD seam: request serialization through the public UserAuth form, plus the shared explicit-locale accessor. Tests cover a persisted Spanish choice winning over an `en-US` browser locale and missing/invalid values being omitted.
- Verification: focused UserAuth/language tests `8 passed`; isolated localization contract suite `28 passed` with `--testTimeout=30000`; TypeScript/build passed; catalog-file Prettier checks passed; `git diff --check` passed. The full Vitest run reached `477 passed` and timed out only the two repository-wide source scans at their default 5-second limit; the same scans pass in isolation with the extended timeout.
- Backend evidence remains the reviewed Python 3.11 magic-link suite recorded above; this follow-up does not change the backend request schema or renderer.

## Frontend provenance hardening follow-up

- Spec finding addressed: request propagation must not invoke `hasChosenLanguage()`, because that helper intentionally migrates unmarked legacy `enclave_language` values and those values may have originated from historical browser detection.
- `getExplicitLanguageChoice()` now requires the independent `STORAGE_KEY_LANGUAGE_EXPLICIT === '1'` marker before returning any catalog-backed locale. UI language migration through `hasChosenLanguage()` remains unchanged for its existing callers.
- TDD: the new regression was red before the fix for an unmarked stored `es` value, then passed after the accessor change. Tests also cover an explicit-marker mismatch and preserve marked valid/invalid behavior.
- Verification: focused UserAuth/language tests `10 passed`; isolated localization contract suite `28 passed` with `--testTimeout=30000`; TypeScript/build passed; locale utility Prettier checks passed; `git diff --check` passed.

## Explicit-choice provenance correction

- Spec finding addressed: the first provenance hardening still allowed `InstanceConfigContext` and `UserOnboarding` to call the legacy migration path, which could turn an unmarked browser-cached `enclave_language` value into an explicit request locale before `UserAuth` ran.
- `hasChosenLanguage()` now reports only the independently written explicit marker. `UserOnboarding` may still honor an unmarked valid cached language for returning-user display/navigation, but no longer writes the marker from that path. Only direct `LanguageSwitcher` selection and the onboarding Continue action call `saveExplicitLanguageChoice()`.
- TDD regressions cover the real onboarding pre-auth path (unmarked cached `es` does not gain the marker), the real UserAuth request (unmarked locale omitted), marked explicit propagation, marker mismatch, and missing/invalid values.
- Verification: impacted language/onboarding/auth tests `14 passed`; isolated localization contract suite `28 passed` with `--testTimeout=30000`; TypeScript/build passed; locale utility Prettier checks passed; `git diff --check` passed.

## Final onboarding provenance correction

- Standards finding addressed: an unmarked cached locale must not redirect directly to auth, because that could show a non-English auth UI while the email request correctly fell back to English.
- `UserOnboarding` now redirects returning Users only when `hasChosenLanguage()` sees the explicit marker. An unmarked cached locale remains in the language-selection flow; direct selection and Continue still write the marker, keeping UI and magic-link request locale aligned. Marked returning choices retain the redirect behavior.
- TDD regressions cover unmarked cached `es` staying on selection with no marker, marked `es` redirecting, and direct Continue preserving explicit marker creation.
- Verification: impacted onboarding/auth/language/LanguageSwitcher tests `19 passed`; isolated localization contract suite `28 passed` with `--testTimeout=30000`; TypeScript/build passed; locale utility/onboarding Prettier checks passed; `git diff --check` passed.
