# Issue 660 session

## Issue

- Issue: #660, Make one locale catalog enforce every advertised language
- Fixed point before session: `c76b02a1829155666f7412f73fb43e0061b9951b`
- Worker session: `/root/ticket_660`
- Commits: `1111324`, `043fa3e`, `82886c9`
- Status: complete; independent Standards and Spec reviews passed

## Inputs

- Spec issue: #659
- Ticket: #660
- Relevant glossary terms: Advertised Locale, Locale Structural Completeness, Linguistic Review
- Relevant ADRs: none
- Prototype answer and source branch, if any: none

## Implementation

- Public interface used: advertised locale catalog and localization contract test suite
- Behaviors covered: one typed 31-entry locale catalog; catalog-derived i18next resources, onboarding metadata, and Admin Instance default choices; frontend/Enclave Control Plane locale-code structural agreement; all-locale missing-key, placeholder, and plural inspection; exact #662/#663 catalog debt; binding-aware AST enforcement for production translation calls and dynamic-template registration; explicit #661 dynamic Readiness debt
- `tdd` used: yes, at the catalog/contract seams confirmed in #659
- Commands run during implementation:
  - `cd frontend && npm ci`
  - `cd frontend && npm test -- --run src/i18n/localeCatalog.test.ts`
  - `cd frontend && npm test -- --run src/pages/AdminInstanceConfig.test.tsx -t 'offers every Advertised Locale'`
  - `cd frontend && npm test -- --run src/i18n/localizationContract.test.ts`
  - `cd frontend && npm test -- --run src/i18n/ src/pages/AdminInstanceConfig.test.tsx src/components/admin/CoveragePicker.test.tsx src/components/reachout/ReachoutModal.test.tsx src/pages/AdminUserConfig.test.tsx` (7 files, 41 tests passed)
  - `cd frontend && npx tsc --noEmit` (passed)
  - `cd frontend && npm run build` (passed)
  - `scripts/test_frontend_runtime.sh` (container build and 5 HTTP/runtime tests passed)
  - `cd frontend && npm test -- --run src/i18n/localizationContract.test.ts` (15 tests passed after review fixes)
  - `cd frontend && npm test -- --run src/i18n/ src/pages/AdminUserConfig.test.tsx src/components/reachout/ReachoutModal.test.tsx src/components/admin/CoveragePicker.test.tsx` (6 files, 39 tests passed after review fixes)
  - `cd frontend && npx tsc --noEmit` (passed after review fixes)
  - `cd frontend && npm run build` (passed after review fixes)
  - `cd frontend && if rg -l 'createSourceFile|ScriptTarget\.Latest' dist/assets; then exit 1; fi` (passed; TypeScript compiler code absent from production assets)
  - `cd frontend && npm test -- --run src/i18n/localizationContract.test.ts src/pages/AdminAIConfig.test.tsx` (2 files, 29 tests passed after binding-analysis review fix)
  - `cd frontend && npx tsc --noEmit` (passed after binding-analysis review fix)
  - `cd frontend && npm run build` (passed after binding-analysis review fix)
  - `cd frontend && if rg -l 'createSourceFile|ScriptTarget\.Latest' dist/assets; then exit 1; fi` (passed after binding-analysis review fix)
- Full suite command: `cd frontend && npm test -- --run`
- Full suite result: 81 files, 457 tests passed

## Review

- Review fixed point: `c76b02a1829155666f7412f73fb43e0061b9951b`
- Standards findings: runtime consumers duplicated localized field types and reachout modes instead of using the dynamic-family source of truth
- Spec findings: the original exact-callee regex could miss aliased or optional dynamic translation calls; the first AST revision inspected unrelated callees and did not reject indirect translation-key expressions; count-only locale-debt assertions could pass when one missing key replaced another
- Worthy fixes applied: replaced the scanner with binding-aware AST analysis for `useTranslation` bindings, typed `TFunction` wrappers, optional calls, and direct `i18n.t`; rejected variable, concatenated, and wrapper-forwarded keys; ignored unrelated dynamic callees; inlined the three Admin AI tool-label keys exposed by the production contract; pinned the shared 876-key debt and every locale's exact missing plural forms; reused registry constants in runtime field and reachout consumers
- Findings ignored with reasons: guide indices and Enclave Control Plane Deployment Readiness values remain registered at the frontend contract boundary because their authoritative values live across module/runtime boundaries; moving them would expand beyond #660
- Final result: Standards pass; Spec pass after two TDD review-fix rounds

## Risks

- Twenty-five catalogs remain exactly 876 English keys behind and have locale-specific plural debt. The contract exposes that known debt until #662/#663 remove it.
- Sixteen reachable Deployment Readiness summary/next-action keys are registered but intentionally absent from English until #661 closes that server/static-copy slice.
